apiVersion: v1
kind: Pod
metadata:
  name: dns-health
  namespace: kube-system
spec:
  containers:
  - name: ubuntu
    image: ubuntu:latest
    command: ["/bin/bash", "-c", "--"]
    args: ["while true; do sleep 3600; done"]
  serviceAccountName: dns-health-sa
